RSpec.describe ActiveModel::LoggerAttributes do
  it "has a version number" do
    expect(ActiveModel::LoggerAttributes::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
